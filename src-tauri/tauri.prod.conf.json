{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBuildCommand": "npm run build && npm run build:sidecar"
  },
  "bundle": {
    "externalBin": [
      "binaries/code-atlas-node"
    ],
    "resources": {
      "../dist-sidecar/": "backend/"
    }
  }
}
